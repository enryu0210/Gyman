name: gyman
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 상태관리 — develop_plan.md §0 결정
  flutter_riverpod: ^2.5.0

  # 선언적 라우팅 — 역할 분기(트레이너/회원/관리자)에 적합
  go_router: ^14.0.0

  # Supabase 클라이언트 (Auth + DB + Realtime)
  supabase_flutter: ^2.5.0

  # 환경변수(.env) 로딩 — Supabase URL/anon key 코드 분리
  flutter_dotenv: ^5.1.0

  # 날짜/시간 포맷 — "다음 수업까지 N일" 등
  intl: ^0.19.0

  # 채팅 사진 첨부 — 갤러리/카메라에서 이미지 선택 (S2 / 2.3, develop_plan §0)
  # 수업 영상 업로드(촬영/갤러리 선택)에도 재사용 — pickVideo(maxDuration)
  image_picker: ^1.1.0

  # 수업 영상 재생 — 비공개 버킷 서명 URL 을 인라인 재생 (S 시리즈, develop_plan §0)
  video_player: ^2.9.1

  # PT 시작 전 로컬 알림 (운톡 P1 후속, develop_plan §0) — FCM 없이 기기에서
  # 다가올 수업을 예약 알림. timezone 은 zonedSchedule 의 TZDateTime 용.
  # (기기 타임존 탐지는 불필요 — "지금부터 N후" 상대 예약이라 tz.local=UTC 기본값으로 충분)
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4

  # 알림 리드타임 등 기기 로컬 설정 저장 (이미 transitive 존재 → 직접 선언으로 승격)
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # 단위 테스트용 mock (renewal_calculator 등 도메인 로직 테스트에 사용)
  mocktail: ^1.0.0

  # 브랜드 에셋 생성기 (빌드 타임 전용 — 앱에 번들되지 않음).
  # 런처 아이콘/스플래시를 assets/brand/ 소스 PNG에서 네이티브 리소스로 일괄 생성.
  # 재생성: `dart run flutter_launcher_icons` / `dart run flutter_native_splash:create`
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.1

# ── 런처 아이콘 (flutter_launcher_icons) ────────────────────────────────
# 브랜드 = 잉크 블랙(#16181D) 배경 + 볼트 라임(#C6FF00) 번개 (DESIGN.md).
# 안드로이드: adaptive(foreground=번개 / background=잉크) + 구버전 폴백(icon_master).
# iOS: 알파 없는 풀블리드 정사각(icon_ios) — iOS가 자체 squircle 마스킹.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/brand/icon_master.png"      # 안드로이드 구버전(<26) 폴백 겸 iOS 소스
  adaptive_icon_background: "#16181D"
  adaptive_icon_foreground: "assets/brand/icon_foreground.png"
  # ── iOS 전용 ──────────────────────────────────────────────────────────
  # iOS 는 전용 소스(icon_ios.png)를 쓴다. icon_master 를 그대로 주면 안 되는 이유:
  #   1) 알파 채널이 있으면 App Store Connect 가 업로드 자체를 거부한다
  #   2) iOS 가 자체 squircle 마스크를 씌우는데 이미 둥근 아이콘을 주면 이중 라운딩이 된다
  # icon_ios.png 는 icon_master 를 잉크(#16181D) 위에 알파 합성해 24bpp 로 평탄화한 것 —
  # "잉크 풀블리드 정사각 + 가운데 볼트"라 iOS 마스크와 정확히 맞물린다.
  #
  # ⚠ remove_alpha_ios + background_color_ios 로 이걸 대신하려 했으나
  #    flutter_launcher_icons 0.14.4 에 버그가 있다 — 원래 모서리 곡선을 따라
  #    녹색 채널이 터진 픽셀(23,254,28)이 1200개 넘게 남는다(2026-08-01 실측).
  #    그래서 소스를 미리 평탄화하고 알파 제거 기능은 쓰지 않는다.
  image_path_ios: "assets/brand/icon_ios.png"

# ── 스플래시 (flutter_native_splash) ────────────────────────────────────
# 라이트/다크 공통 잉크 배경(브랜드 스플래시는 항상 다크). 로고=번개+GYMAN 워드마크.
# 안드로이드 12+는 OS 규격상 아이콘만 노출 → 번개 마크(splash_bolt)만 사용.
flutter_native_splash:
  color: "#16181D"
  color_dark: "#16181D"
  image: assets/brand/splash_logo.png
  image_dark: assets/brand/splash_logo.png
  android_12:
    image: assets/brand/splash_bolt.png
    icon_background_color: "#16181D"
    image_dark: assets/brand/splash_bolt.png
    icon_background_color_dark: "#16181D"
  android: true
  ios: true
  web: false                                       # 웹은 정식 배포 경로 아님(대기 기간용 임시 우회로만)

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true

  # .env 파일을 자산으로 등록 — flutter_dotenv가 런타임에 읽음
  # 실제 .env는 .gitignore에 들어가고, .env.example만 커밋됨
  assets:
    - .env

  # Pretendard — 한국어 앱 표준 가변폰트(OFL). 전 weight를 단일 파일로 커버.
  # 라이선스 전문은 assets/fonts/OFL.txt 참고. google_fonts 미사용(오프라인 동작·deps 지침).
  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/PretendardVariable.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
