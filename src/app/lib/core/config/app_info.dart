/// 앱 메타 정보 상수.
///
/// pubspec.yaml 의 version 과 **수동 동기화** — package_info_plus 의존성을 추가하지
/// 않기 위해 상수로 둔다(develop_plan §0 의존성 고정). 버전 올릴 때 같이 갱신할 것.
///
/// 약관/개인정보 버전은 문서 개정 추적용 — 본문을 고치면 날짜를 올려 재동의를
/// 유도할 수 있다(user_consents 테이블, 0034/0041).
library;

/// 앱 표시 버전. 문의 전송 시 함께 보내 재현 추적에 사용.
/// ⚠ pubspec.yaml `version:` 과 일치시킬 것.
const String kAppVersion = '1.0.0';

/// 이용약관 버전(개정일). 본문 수정 시 함께 올린다.
/// 2026-08-01: 제6조 탈퇴 시 삭제·보존 범위를 실제 동작에 맞게 재작성.
/// 2026-08-10: 미가입 회원 정의(제2조 4항)·트레이너 동의 취득 의무(제5조 3항)·
///             서비스 종료(제8조)·준거법(제9조)·운영자 표기(부칙) 추가.
const String kTermsVersion = '2026-08-10';

/// 개인정보 처리방침 버전(개정일). 본문 수정 시 함께 올린다.
/// 2026-08-01: 제3항 보유기간을 실제 동작에 맞게 재작성
/// (회원이 올린 것은 삭제 / 트레이너 기록물은 보존 + 영상·사진의 식별 가능성 고지).
/// 2026-08-10: 수집 항목에 사진·영상·트레이너 메모·소셜 로그인 추가,
///             민감정보(제3항)·위탁(제6항)·국외이전(제7항)·만14세미만(제8항)·
///             미가입 회원(제9항)·보호책임자(제12항) 신설.
const String kPrivacyVersion = '2026-08-10';

/// 민감정보(건강정보) 처리 동의 버전(개정일).
///
/// **약관·처리방침과 별도 상수인 이유:** 민감정보 동의는 법적으로 분리해서 받아야
/// 하는 별개의 동의라(선택 항목), 처리방침을 고칠 때마다 같이 올라가면
/// "무엇에 동의했는지"가 흐려진다. 민감정보 항목 자체가 바뀔 때만 올린다.
const String kSensitiveConsentVersion = '2026-08-10';

/// 개인정보 보호책임자 성명.
///
/// ⚠ **앱스토어 등록 전 반드시 실명으로 교체할 것.** 사업자 등록 전이라
/// 개인 운영자 명의로 기재한다(2026-08-10 사용자 확인). 미기재 상태면 약관
/// 화면에 placeholder 가 그대로 보이도록 두었다 — 조용히 빈칸이 되면
/// 심사 단계에서야 발견하게 되기 때문이다.
const String kOperatorName = '{{운영자_성명}}';

/// 개인정보 관련 문의를 받을 연락처(이메일).
/// ⚠ 위와 동일 — 앱스토어는 **웹에서 접근 가능한 연락 수단**을 요구하므로
/// 앱 내 [문의하기]로 갈음할 수 없다.
const String kOperatorContact = '{{연락처_이메일}}';
